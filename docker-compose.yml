version: "3.3"

services:

  # ==========================
  # Optional VPN (Gluetun)
  # ==========================
  #gluetun:
  #  image: qmcgaw/gluetun:v3.34
  #  container_name: gluetun
  #  cap_add:
  #    - NET_ADMIN
  #  devices:
  #    - /dev/net/tun:/dev/net/tun
  #  environment:
  #    - VPN_SERVICE_PROVIDER=
  #    - VPN_TYPE=
  #    - OPENVPN_USER=
  #    - OPENVPN_PASSWORD=
  #    - WIREGUARD_PRIVATE_KEY=
  #    - WIREGUARD_ADDRESS=
  #    - WIREGUARD_PUBLIC_KEY=
  #    - TZ=Asia/Kolkata
  #    - UPDATER_PERIOD=24h
  #  ports:
  #    - "80:80"
  #    - "8080:8080"
  #  restart: unless-stopped

  # ==========================
  # Main APP (Your Leech Bot)
  # ==========================
  app:
    container_name: azml2
    image: azml2
    build: .
    command: bash start.sh
    restart: unless-stopped

    # If using Gluetun, uncomment:
    #network_mode: "service:gluetun"

    ports:
      - "11677:85"

    environment:
      - DOWNLOAD_DIR=/usr/src/app/downloads/
      - PORT=85

    env_file:
      - .env
